"""
fidelity_metrics.py

– Wrap qutip.metrics functions:
    • fidelity(state1, state2)
    • process_fidelity(U_target, U_sim)
    • average_gate_fidelity(...)
– Compute and return numeric metrics for training and benchmarking
"""
